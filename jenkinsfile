node {
    agent { dockerfile true }
        stage('Testing') {
           sshagent (['k8s-1']) {
              sh '''
               sodo docker run --privileged -d --name dind-test docker:dind
               sudo docker ps
               '''
            }
        }
}
