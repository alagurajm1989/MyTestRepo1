node {
    agent { dockerfile true }
        stage('Testing') {
           sshagent (['k8s-1']) {
              sh '''
               docker run --privileged -d --name dind-test docker:dind
               docker ps
               '''
            }
        }
}
